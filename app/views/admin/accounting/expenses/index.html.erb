<div class="space-y-6" data-controller="approval accounting-expense-detail">
  <div class="flex justify-between items-center">
    <div><h1 class="text-2xl font-bold">経費・仕入管理</h1><p class="text-sm text-gray-500">承認処理と支払管理</p></div>
    <div class="flex gap-2">
      <button class="btn-secondary text-sm">経費登録</button>
      <button class="btn-secondary text-sm">仕入登録</button>
      <button class="btn-primary text-sm" data-action="click->approval#bulkApprove">一括承認</button>
    </div>
  </div>
  <div class="grid grid-cols-2 gap-4 sm:grid-cols-5">
    <div class="card p-4"><p class="text-xs text-gray-500">未承認経費</p><p class="mt-2 text-3xl font-bold text-orange-600"><%= @summary[:pending_expenses] %></p></div>
    <div class="card p-4"><p class="text-xs text-gray-500">未払仕入</p><p class="mt-2 text-3xl font-bold text-orange-600"><%= @summary[:pending_purchases] %></p></div>
    <div class="card p-4"><p class="text-xs text-gray-500">月次経費</p><p class="mt-2 text-xl font-bold"><%= number_to_currency(@summary[:monthly_expense_total], unit: "¥", precision: 0) %></p></div>
    <div class="card p-4"><p class="text-xs text-gray-500">月次仕入</p><p class="mt-2 text-xl font-bold"><%= number_to_currency(@summary[:monthly_purchase_total], unit: "¥", precision: 0) %></p></div>
    <div class="card p-4"><p class="text-xs text-gray-500">承認率</p><p class="mt-2 text-3xl font-bold text-green-600"><%= @summary[:approval_rate] %>%</p></div>
  </div>
  <div class="bg-white rounded-xl shadow-sm overflow-hidden">
    <div class="px-6 py-4 border-b"><h3 class="font-bold">経費申請一覧</h3></div>
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50"><tr>
        <th class="px-4 py-3"><input type="checkbox" data-action="change->approval#selectAll"></th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">申請日</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">申請者</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">費目</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">内容</th>
        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">金額</th>
        <th class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase">領収書</th>
        <th class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase">ステータス</th>
        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">操作</th>
      </tr></thead>
      <tbody class="divide-y divide-gray-200">
        <% @expense_requests.each do |exp| %>
        <tr class="hover:bg-gray-50 cursor-pointer"
            data-action="click->accounting-expense-detail#open"
            data-accounting-expense='<%= exp.to_json %>'>
          <td class="px-4 py-4"><input type="checkbox" value="<%= exp[:id] %>" data-approval-target="checkbox" data-action="change->approval#toggleSelection"></td>
          <td class="px-6 py-4 text-sm"><%= exp[:request_date] %></td>
          <td class="px-6 py-4 text-sm"><%= exp[:employee_name] %></td>
          <td class="px-6 py-4 text-sm"><%= exp[:category] %></td>
          <td class="px-6 py-4 text-sm"><%= exp[:description] %></td>
          <td class="px-6 py-4 text-sm text-right font-medium"><%= number_to_currency(exp[:amount], unit: "¥", precision: 0) %></td>
          <td class="px-6 py-4 text-center"><%= exp[:receipt] ? '✓' : '-' %></td>
          <td class="px-6 py-4 text-center">
            <% case exp[:status] %>
            <% when "承認待ち" %>
              <span class="px-2 py-1 text-xs font-semibold rounded-full bg-yellow-100 text-yellow-800">承認待ち</span>
            <% when "承認済" %>
              <span class="px-2 py-1 text-xs font-semibold rounded-full bg-green-100 text-green-800">承認済</span>
            <% when "差戻し" %>
              <span class="px-2 py-1 text-xs font-semibold rounded-full bg-red-100 text-red-800">差戻し</span>
            <% end %>
          </td>
          <td class="px-6 py-4 text-right text-sm">
            <% if exp[:status] == "承認待ち" %>
              <button class="text-green-600 hover:text-green-800 mr-2" data-action="click->approval#approve" data-item-id="<%= exp[:id] %>" data-item-name="<%= exp[:description] %>">承認</button>
              <button class="text-red-600 hover:text-red-800" data-action="click->approval#reject" data-item-id="<%= exp[:id] %>" data-item-name="<%= exp[:description] %>">差戻</button>
            <% end %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="hidden fixed bottom-4 right-4 bg-white shadow-lg rounded-lg p-4 border-2 border-blue-500" data-approval-target="bulkToolbar">
    <div class="flex items-center gap-4">
      <span class="text-sm font-medium"><span data-approval-target="selectedCount">0</span>件選択中</span>
      <button class="btn-primary text-sm" data-action="click->approval#bulkApprove">一括承認</button>
      <button class="btn-secondary text-sm" data-action="click->approval#bulkReject">一括差戻</button>
    </div>
  </div>

  <!-- 詳細ペインオーバーレイ -->
  <div class="fixed inset-0 bg-gray-900/50 z-40 hidden"
       data-accounting-expense-detail-target="overlay"
       data-action="click->accounting-expense-detail#close"></div>

  <!-- 詳細ペイン -->
  <aside class="fixed inset-y-0 right-0 w-full md:w-2/3 lg:w-1/2 xl:w-1/3 bg-white shadow-2xl transform translate-x-full transition-transform duration-300 ease-in-out z-50 overflow-y-auto"
         data-accounting-expense-detail-target="panel">
    <!-- ヘッダー -->
    <div class="sticky top-0 bg-white border-b border-gray-200 px-6 py-4 z-10">
      <div class="flex items-center justify-between">
        <h2 class="text-xl font-bold text-gray-900">経費詳細</h2>
        <button class="text-gray-400 hover:text-gray-600 p-2"
                data-action="click->accounting-expense-detail#close">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>

    <!-- コンテンツ -->
    <div class="p-6 space-y-6">
      <!-- 経費情報 + ステータス -->
      <div class="flex items-start justify-between">
        <div>
          <h3 class="text-2xl font-bold text-gray-900 mb-2" data-accounting-expense-detail-target="description"></h3>
          <p class="text-sm text-gray-500">
            申請者: <span data-accounting-expense-detail-target="employeeName"></span>
          </p>
        </div>
        <span class="inline-flex px-3 py-1 text-sm font-semibold rounded-full" data-accounting-expense-detail-target="statusBadge"></span>
      </div>

      <!-- クイックアクション -->
      <div class="flex space-x-2">
        <button class="flex-1 btn-primary text-sm">
          <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          承認
        </button>
        <button class="flex-1 btn-secondary text-sm">
          <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
          差戻
        </button>
      </div>

      <!-- 基本情報 -->
      <div class="space-y-4">
        <h4 class="font-semibold text-gray-900 flex items-center">
          <svg class="w-5 h-5 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
          </svg>
          基本情報
        </h4>

        <dl class="grid grid-cols-2 gap-4">
          <div>
            <dt class="text-sm font-medium text-gray-500">申請日</dt>
            <dd class="mt-1 text-sm text-gray-900" data-accounting-expense-detail-target="requestDate"></dd>
          </div>
          <div>
            <dt class="text-sm font-medium text-gray-500">費目</dt>
            <dd class="mt-1 text-sm text-gray-900" data-accounting-expense-detail-target="category"></dd>
          </div>
          <div>
            <dt class="text-sm font-medium text-gray-500">金額</dt>
            <dd class="mt-1 text-lg font-bold text-gray-900" data-accounting-expense-detail-target="amount"></dd>
          </div>
          <div>
            <dt class="text-sm font-medium text-gray-500">領収書</dt>
            <dd class="mt-1 text-sm text-gray-900" data-accounting-expense-detail-target="receipt"></dd>
          </div>
        </dl>
      </div>

      <!-- 経費詳細 -->
      <div class="space-y-4">
        <h4 class="font-semibold text-gray-900 flex items-center">
          <svg class="w-5 h-5 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          経費詳細
        </h4>

        <dl class="grid grid-cols-1 gap-4">
          <div>
            <dt class="text-sm font-medium text-gray-500">内容</dt>
            <dd class="mt-1 text-sm text-gray-900" data-accounting-expense-detail-target="descriptionDetail"></dd>
          </div>
          <div>
            <dt class="text-sm font-medium text-gray-500">申請者</dt>
            <dd class="mt-1 text-sm text-gray-900" data-accounting-expense-detail-target="employeeNameDetail"></dd>
          </div>
        </dl>
      </div>

      <!-- 承認情報 -->
      <div class="space-y-4">
        <h4 class="font-semibold text-gray-900 flex items-center">
          <svg class="w-5 h-5 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          承認情報
        </h4>

        <dl class="grid grid-cols-1 gap-4">
          <div>
            <dt class="text-sm font-medium text-gray-500">承認者</dt>
            <dd class="mt-1 text-sm text-gray-900" data-accounting-expense-detail-target="approver">未承認</dd>
          </div>
          <div>
            <dt class="text-sm font-medium text-gray-500">承認日</dt>
            <dd class="mt-1 text-sm text-gray-900" data-accounting-expense-detail-target="approvedDate">-</dd>
          </div>
          <div>
            <dt class="text-sm font-medium text-gray-500">支払予定日</dt>
            <dd class="mt-1 text-sm text-gray-900" data-accounting-expense-detail-target="paymentDate">-</dd>
          </div>
        </dl>

        <!-- ステータスアラート -->
        <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4" data-accounting-expense-detail-target="statusAlert">
          <div class="flex">
            <svg class="w-5 h-5 text-yellow-600 mr-3" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
            </svg>
            <div>
              <p class="text-sm font-medium text-yellow-800">承認待ち</p>
              <p class="text-sm text-yellow-700 mt-1">この経費申請は承認待ちです。速やかに承認処理を行ってください。</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </aside>
</div>
